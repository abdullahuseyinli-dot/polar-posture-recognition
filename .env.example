# Point this at a manifest with image_path, label, and split columns.
HAC_MANIFEST_PATH=C:/path/to/standardized_manifest.csv

# Optional local output location. Repository defaults keep bulky runs untracked.
HAC_ARTIFACT_ROOT=.runs/local
