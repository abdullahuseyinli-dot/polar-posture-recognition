[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "polar-posture-recognition"
version = "1.1.0"
description = "Audited four- and nine-class POLAR posture benchmarks with reproducible prediction evidence"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "imagehash>=4.3,<5",
  "joblib>=1.4,<2",
  "matplotlib>=3.9,<4",
  "numpy>=2.0,<3",
  "pandas>=2.2,<4",
  "pillow>=11,<13",
  "grad-cam>=1.5,<2",
  "scikit-learn>=1.6,<2",
  "scipy>=1.14,<2",
  "seaborn>=0.13,<1",
  "torch>=2.6,<3",
  "torchvision>=0.21,<1",
  "tqdm>=4.66,<5",
  "transformers==5.5.3",
]

[project.urls]
Repository = "https://github.com/abdullahuseyinli-dot/polar-posture-recognition"
Documentation = "https://github.com/abdullahuseyinli-dot/polar-posture-recognition/tree/main/docs"
"Companion architecture" = "https://github.com/abdullahuseyinli-dot/arftr"

[project.optional-dependencies]
notebook = [
  "ipykernel>=6.29,<8",
  "jupyterlab>=4.3,<5",
  "nbclient>=0.10,<1",
  "nbformat>=5.10,<6",
]
dev = ["pytest>=8.3,<10", "ruff==0.16.6"]
report = [
  "pdfplumber>=0.11,<1",
  "pypdf>=6,<7",
  "reportlab>=5,<6",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"experiments/pipeline_source.ipynb" = [
  "E402",
  "F401",
  "F811",
  "F841",
  "I001",
  "UP015",
  "UP038",
]

[tool.ruff.format]
exclude = ["experiments/pipeline_source.ipynb"]
